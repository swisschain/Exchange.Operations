apiVersion: v1
kind: Service
metadata:
  labels:
    app: operations-worker
    environment: Development
  name: operations-worker
  namespace: exchange
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 5000
  selector:
    app: operations-worker
  sessionAffinity: None
  type: ClusterIP
